<%= form_for([@group, @chat], class: 'form-inline') do|f|%>
  <div class="controls controls-row">
    <%= f.text_area :content, class: 'span10 auto_grow', rows: '1' %>
    <%= f.submit t('chats._form.submit'), class:'btn btn-primary span2' %>
  </div>
<% end %>
