<%= form_tag email_path, method: :put do %>
  <div class="controls controls-row">
    <%= text_field_tag :email, nil, class: 'span10' %>
    <%= submit_tag t('action.update'), class: 'span2 btn' %>
  </div>
<% end %>
