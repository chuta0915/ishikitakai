<ul class="breadcrumb">
  <li><%= link_to t("nav.groups"), groups_path %></a> <span class="divider">/</span></li>
  <li><%= link_to @group.name, url_for(@group) %> <span class="divider">/</span></li>
  <li class="active"><%= t('nav.kpt') %></li>
</ul>
<%= render 'form' %>
<div class="row-fluid">
  <div class="span4">
    <h2>KEEP</h2>
    <ul class="unstyled">
    <% @keeps.each do |kpt| %>
      <li class="well">
        <%= kpt.name %>
        <%= link_to kpt_path(@group, kpt), method: :delete, class: 'close' do %>
          &times;
        <% end %> 
      </li>
    <% end %>
    </ul>
  </div>
  <div class="span4">
    <h2>PROBLEM</h2>
    <ul class="unstyled">
    <% @problems.each do |kpt| %>
      <li class="well">
        <%= kpt.name %>
        <%= link_to kpt_path(@group, kpt), method: :delete, class: 'close' do %>
          &times;
        <% end %> 
      </li>
    <% end %>
  </ul>
  </div>
  <div class="span4">
    <h2>TRY</h2>
    <ul class="unstyled">
    <% @trys.each do |kpt| %>
      <li class="well">
        <%= kpt.name %>
        <%= link_to kpt_path(@group, kpt), method: :delete, class: 'close' do %>
          &times;
        <% end %> 
      </li>
    <% end %>
  </ul>
  </div>
</div>
